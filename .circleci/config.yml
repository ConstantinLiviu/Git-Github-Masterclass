version: 2.1
jobs:
  build_and_test:
    docker:
      - image: cimg/node:20.0.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm Install
      - run:
          name: Run automated tests
          command: npm tests

workflows:
  sample:
    jobs:
      - build_and_test
